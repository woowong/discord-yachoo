## 1. Serializer Implementation

- [x] 1.1 Define the `DICE_ROLL_GIPHY_POOL` constant array in `src/presentation/discord/adapter/serializer.ts` with all 7 Giphy URLs.
- [x] 1.2 Update the `serializeRolling` method in `src/presentation/discord/adapter/serializer.ts` to pick a random Giphy URL from `DICE_ROLL_GIPHY_POOL`.

## 2. Verification

- [x] 2.1 Run the existing tests using `npm run test` to verify that no regressions were introduced.
